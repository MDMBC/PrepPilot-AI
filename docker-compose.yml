services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: preppilot-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: preppilot_ai
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
